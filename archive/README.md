# Archive

This folder keeps legacy assets that are not part of the current product runtime.

Current product runtime scope:
- `api/`
- `web/`
- ETL pipeline under `src/`, `sql/`, `sqlm/`

Legacy preserved here:
- `archive/dash/` (old Superset sandbox files)
