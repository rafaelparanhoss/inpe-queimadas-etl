name: daily

on:
  schedule:
    - cron: "30 9 * * *"
  workflow_dispatch:
    inputs:
      date:
        description: "override date (YYYY-MM-DD)"
        required: false
        type: string

jobs:
  run:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ${{ github.workspace }}
    env:
      TZ: America/Sao_Paulo
      DB_CONTAINER: geoetl_postgis
      DB_USER: geoetl
      DB_NAME: geoetl
    steps:
      - uses: actions/checkout@v4

      - name: set up python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: install uv
        run: python -m pip install uv

      - name: install deps
        run: |
          python -m uv sync
          python -m uv pip install -e .

      - name: make scripts executable
        run: |
          chmod +x scripts/*.sh
          if [ -f ./run_today.sh ]; then
            chmod +x ./run_today.sh
          fi
          if [ -f ./report_day.sh ]; then
            chmod +x ./report_day.sh
          fi

      - name: resolve date
        id: run_date
        run: |
          if [ -n "${{ github.event.inputs.date }}" ]; then
            date_str="${{ github.event.inputs.date }}"
            source="input"
          else
            date_str="$(TZ=America/Sao_Paulo date +%F)"
            source="tz"
          fi

          if ! printf '%s' "$date_str" | grep -Eq '^[0-9]{4}-[0-9]{2}-[0-9]{2}$'; then
            echo "invalid date: $date_str (expected YYYY-MM-DD)" >&2
            exit 1
          fi

          echo "date=$date_str" >> "$GITHUB_OUTPUT"
          echo "source=$source" >> "$GITHUB_OUTPUT"

      - name: start db
        run: |
          set -e
          if docker compose up -d --wait --wait-timeout 120; then
            exit 0
          fi

          echo "docker compose --wait unavailable, fallback to manual wait"
          docker compose up -d

          for i in $(seq 1 60); do
            status="$(docker inspect -f '{{.State.Health.Status}}' "$DB_CONTAINER" 2>/dev/null || true)"
            if [ "$status" = "healthy" ]; then
              exit 0
            fi
            if docker exec "$DB_CONTAINER" pg_isready -U "$DB_USER" -d "$DB_NAME"; then
              exit 0
            fi
            sleep 2
          done

          docker compose ps
          docker logs "$DB_CONTAINER"
          exit 1

      - name: run pipeline
        run: |
          date_str="${{ steps.run_date.outputs.date }}"
          if [ "${{ steps.run_date.outputs.source }}" = "input" ]; then
            bash scripts/run_all.sh --date "$date_str" --checks
          else
            if [ -f scripts/run_today.sh ]; then
              bash scripts/run_today.sh --date "$date_str"
            elif [ -f ./run_today.sh ]; then
              bash ./run_today.sh --date "$date_str"
            else
              echo "run_today.sh not found" >&2
              exit 1
            fi
          fi

      - name: run report
        run: |
          date_str="${{ steps.run_date.outputs.date }}"
          if [ -f scripts/report_day.sh ]; then
            bash scripts/report_day.sh --date "$date_str"
          elif [ -f ./report_day.sh ]; then
            bash ./report_day.sh --date "$date_str"
          else
            echo "report_day.sh not found" >&2
            exit 1
          fi

      - name: upload artifacts
        uses: actions/upload-artifact@v4
        if: ${{ always() }}
        with:
          name: reports-and-logs
          path: |
            data/logs/**
            data/reports/**
          if-no-files-found: warn
